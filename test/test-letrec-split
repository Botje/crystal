(define x 5)
(define (f y) (+ y 1))
(define (fac n) (+ 1 (fac n)))

(define (even n)
	(if (= n 0)
		#t
		(not (odd (- n 1)))))

(define (odd n)
  (if (= n 0)
	  #f
		(not (even (- n 1)))))

(even 42)
