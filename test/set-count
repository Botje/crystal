(let* ((counter 0)
       (add! (lambda (x)
        (let ((new-counter (+ counter x)))
          (set! counter new-counter)))))
  (display counter)
  (add! 5)
  (display counter)
  (add! 2)
  (display counter))
